<div{{ attributes.addClass(classes) }}>
	<div{{ title_attributes.addClass(title_classes) }}>{{ label }}</div>
	<div class="field__items heading">
		{% for item in items %}
			<h2{{ item.attributes.addClass('page-header') }}>{{ item.content }}</h2>
		{% endfor %}
	</div>
</div>
